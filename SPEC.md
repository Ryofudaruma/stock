# 株価下落通知ツール 開発仕様書

> **Claude Code への指示**
> このドキュメントは実装依頼の仕様書です。以下の要件に沿ってプロジェクト一式を実装してください。
> - 「確定事項」と書かれた技術選定は勝手に変更しないこと。変更が必要と判断した場合は、実装を始める前に理由とともに提案し、承認を得ること。
> - 仕様に曖昧な点や矛盾を見つけた場合は、推測で進めずに質問すること。
> - 実装完了後、必ず「12. 受け入れ基準」のチェックリストを自分で検証し、結果を報告すること。
> - 認証情報(トークン・パスワード)は絶対にコード内にハードコードせず、コミットもしないこと。

---

## 1. 概要

指定した日本株・米国株の株価を定期的にチェックし、あらかじめ設定した金額を下回ったら LINE とメールで通知するツールを作る。

利用者のPCを常時起動する必要がないよう、GitHub Actions 上で定期実行する構成とする。個人利用が前提で、無料または極めて低コストで運用できることを重視する。

## 2. スコープ

### 対象
- 日本株(東証上場銘柄)
- 米国株(NYSE / NASDAQ 上場銘柄)

### 対象外(今回は実装しない)
- 暗号資産、投資信託、FX
- 株価チャートの表示、UI・Webフロントエンド
- 自動売買・証券会社口座との連携
- 「閾値を上回ったら通知」などの上昇方向のアラート
- 複数ユーザーへの通知(通知先は利用者本人1名のみ)

## 3. 技術スタック(確定事項)

| 項目 | 採用技術 | 備考 |
|------|----------|------|
| 言語 | Python 3.11 | |
| 株価取得 | `yfinance` | Yahoo Finance の非公式ライブラリ。無料・APIキー不要 |
| 設定ファイル | YAML (`PyYAML`) | |
| HTTP通信 | `requests` | |
| 実行基盤 | GitHub Actions (scheduled workflow) | |
| LINE通知 | LINE Messaging API (push message) | LINE Notify は 2025年に終了済みのため使用しない |
| メール通知 | SMTP (標準ライブラリ `smtplib`) | Gmail + アプリパスワードを想定 |

株価取得処理は、将来 yfinance が使えなくなった場合に別のデータソースへ差し替えられるよう、**単一の関数(例: `get_current_price(symbol) -> float | None`)に隔離**すること。

## 4. ディレクトリ構成

```
.
├── README.md                    # セットアップ手順(利用者向け、日本語)
├── SPEC.md                      # 本ドキュメント
├── requirements.txt
├── config.yaml                  # 監視銘柄・閾値・通知設定
├── state.json                   # 通知済みフラグ(初期値は {})
├── .gitignore
├── src/
│   ├── main.py                  # エントリポイント
│   └── notifiers.py             # LINE / メール送信処理
├── tests/
│   └── test_logic.py            # 閾値判定・重複通知防止のテスト
└── .github/
    └── workflows/
        └── check.yml            # 定期実行ワークフロー
```

## 5. 機能要件

| ID | 要件 |
|----|------|
| FR1 | 監視対象の銘柄(シンボル・表示名・閾値)を `config.yaml` で管理でき、コードを変更せずに銘柄を追加・削除できる |
| FR2 | 実行されるたびに、設定された全銘柄の現在値を取得する |
| FR3 | 現在値が閾値**以下**(`price <= threshold`)の銘柄を検知する |
| FR4 | 検知した銘柄について LINE に通知する(第一の通知手段) |
| FR5 | メール通知にも対応する。既定では LINE 送信に失敗した場合のフォールバックとして送信し、設定で常時併用にも切り替えられる |
| FR6 | 同一銘柄について、同一日(JST基準)に重複して通知しない |
| FR7 | 一度通知した銘柄が閾値を再び上回った場合、当日の通知済みフラグを解除し、再度下回ったときには改めて通知する |
| FR8 | 一部の銘柄で価格取得に失敗しても、処理全体を中断せず、残りの銘柄の処理を継続する |

### シンボルの記法
- 日本株: 証券コード + `.T`(例: トヨタ自動車 = `7203.T`)
- 米国株: ティッカーのみ(例: `AAPL`)
- 閾値は各銘柄の現地通貨建て(日本株なら円、米国株ならドル)。通貨換算は行わない。

## 6. 設定ファイル仕様 (`config.yaml`)

```yaml
notify:
  line: true            # LINE通知を有効にするか
  email: true           # メール通知を有効にするか
  email_always: false   # true なら LINE 成功時もメールを送る / false ならLINE失敗時のみ

stocks:
  - symbol: "7203.T"        # 必須: yfinance のシンボル
    name: "トヨタ自動車"     # 任意: 通知文に使う表示名。省略時は symbol を使う
    threshold: 2500          # 必須: この値以下になったら通知
  - symbol: "AAPL"
    name: "Apple"
    threshold: 180
```

## 7. 状態管理仕様 (`state.json`)

- 形式: `{"<symbol>:<YYYY-MM-DD>": true, ...}` のフラットな JSON オブジェクト
- 日付は **JST (UTC+9)** 基準
- 通知を送った時点でキーを追加する
- 現在値が閾値を上回っていた場合、当日分のキーが存在すれば削除する(FR7 の実現)
- 実行のたびに、7日より古い日付のキーを削除する(ファイルの肥大化防止)
- GitHub Actions のジョブはステートレスなため、このファイルはワークフロー内で **リポジトリにコミット&プッシュして永続化**する

## 8. 通知仕様

### 8.1 LINE (Messaging API)

```
POST https://api.line.me/v2/bot/message/push
Headers:
  Content-Type: application/json
  Authorization: Bearer {LINE_CHANNEL_ACCESS_TOKEN}
Body:
  {"to": "{LINE_USER_ID}", "messages": [{"type": "text", "text": "<本文>"}]}
```

- 本文は 5000 文字が上限。安全のため 4900 文字で切り詰めること
- HTTP 200 を成功と判定し、成功可否を真偽値で返すこと
- 失敗時はエラー内容をログ出力するが、例外を送出してプロセスを落とさないこと

### 8.2 メール (SMTP)

- 既定の接続先は `smtp.gmail.com:587`、STARTTLS を使用
- 件名は `株価アラート`
- 本文は LINE と同じテキスト

### 8.3 通知文のフォーマット

複数銘柄が該当する場合は 1通にまとめて送る(空行区切り)。

```
【株価アラート】トヨタ自動車(7203.T)
現在値: 2450.0
設定した閾値: 2500 を下回りました。
```

## 9. 環境変数 / GitHub Secrets

| 変数名 | 用途 | 必須条件 |
|--------|------|----------|
| `LINE_CHANNEL_ACCESS_TOKEN` | LINE チャネルアクセストークン(長期) | LINE通知を使う場合 |
| `LINE_USER_ID` | 通知先(利用者本人)の userId | LINE通知を使う場合 |
| `SMTP_HOST` | SMTPサーバー | 任意(既定 `smtp.gmail.com`) |
| `SMTP_PORT` | SMTPポート | 任意(既定 `587`) |
| `SMTP_USER` | 送信元メールアドレス | メール通知を使う場合 |
| `SMTP_PASSWORD` | アプリパスワード等 | メール通知を使う場合 |
| `EMAIL_TO` | 通知先メールアドレス | メール通知を使う場合 |

必要な環境変数が未設定の場合は、警告ログを出したうえでその通知手段をスキップし、処理は継続すること(異常終了させない)。

## 10. GitHub Actions ワークフロー仕様

- ファイル: `.github/workflows/check.yml`
- トリガー: `schedule` (cron `*/30 * * * *` = 30分おき) および `workflow_dispatch`(手動実行)
- ランナー: `ubuntu-latest`
- `permissions: contents: write`(state.json のコミットに必要)
- ステップ: checkout → Python 3.11 セットアップ(pipキャッシュ有効) → 依存インストール → `python src/main.py` 実行 → `state.json` に差分があればコミット&プッシュ
- コミットメッセージには `[skip ci]` を含め、コミットが再度ワークフローを誘発しないようにすること
- 差分がない場合にコミットを試みてジョブが失敗しないよう、`git diff --cached --quiet ||` などでガードすること

## 11. エラーハンドリング方針

- 株価取得失敗: 警告ログを出して当該銘柄をスキップ(FR8)。全銘柄失敗でも終了コードは 0 とする
- 通知送信失敗: エラーログを出力。LINE 失敗時はメールにフォールバック
- 想定外の例外でプロセスが落ちると通知が止まったことに気づけないため、個別銘柄の処理は例外を握りつぶして継続する設計にすること

## 12. 受け入れ基準

実装完了後、以下をすべて自分で検証し、結果を報告すること。

- [ ] `python src/main.py` がエラーなく実行でき、各銘柄の現在値と閾値がログに出力される
- [ ] 価格取得をモックしたテストで、閾値以下の銘柄が検知され `state.json` にフラグが立つ
- [ ] 同じ条件で連続実行しても2回目は通知対象にならない(FR6)
- [ ] 価格が閾値を上回った状態で実行すると当日フラグが削除され、再度下回れば通知される(FR7)
- [ ] 環境変数が未設定でも例外で落ちず、スキップの警告ログが出る
- [ ] 一部銘柄の価格取得に失敗しても他の銘柄の処理が継続する(FR8)
- [ ] `config.yaml` と `check.yml` が妥当な YAML としてパースできる
- [ ] リポジトリ内に認証情報が一切含まれていない
- [ ] README.md だけを読めば、利用者がゼロからセットアップして動かせる

## 13. README.md に含めるべき内容

利用者は開発者ではないため、手順は具体的に書くこと。

1. リポジトリを作成して push する手順
2. LINE Developers でプロバイダーと Messaging API チャンネルを作成し、チャネルアクセストークンと自分の userId を取得する手順(LINE Notify が終了しているためこの方式である旨も明記)
3. Gmail のアプリパスワードを発行する手順
4. GitHub Secrets の登録手順
5. `config.yaml` の編集方法(日本株は `.T` 付き、米国株はティッカーそのまま)
6. ローカルでの動作確認方法と、Actions タブからの手動実行方法
7. 注意事項(下記「14. 既知の制約」の内容)

## 14. 既知の制約(README にも記載すること)

- `yfinance` は Yahoo Finance の非公式ライブラリであり、提供側の仕様変更で動作しなくなる可能性がある
- GitHub Actions の scheduled workflow は指定時刻ちょうどの実行が保証されず、負荷状況により数分〜数十分遅延することがある
- 現在の cron 設定では市場が閉じている時間帯も実行される。無駄を減らしたい場合は東証(平日 9:00〜15:30 JST)と米国市場(平日 22:30頃〜翌5:00頃 JST、夏時間で変動)に合わせて cron を絞る
- LINE Messaging API の無料プランには月あたりの送信数上限がある

## 15. 将来の拡張候補(今回は実装しない)

- 市場の営業時間内のみ実行するスケジュール最適化
- 前日比・移動平均など、固定値以外の条件でのアラート
- 通知先の複数化(Slack、Discord 等)
- 通知履歴の蓄積と可視化
