# 株価下落通知ツール 開発仕様書(第2版: Windows 常駐 GUI 版)

> 第1版(GitHub Actions で定期実行し LINE とメールで通知する構成)から、利用者の要望により以下の方針で作り直した。
> - 通知は **メールのみ**(LINE は使わない)
> - GitHub Actions ではなく、**利用者の PC が起動している間だけ** 動く
> - 監視する銘柄を選びやすくする **GUI** を追加する

---

## 1. 概要

指定した日本株・米国株の株価を一定間隔でチェックし、あらかじめ設定した金額以下になったらメールで通知する Windows 用の常駐 GUI アプリ。個人利用が前提で、無料で運用できることを重視する。

## 2. スコープ

### 対象
- 日本株(東証上場銘柄)
- 米国株(NYSE / NASDAQ 上場銘柄)
- Windows 10 / 11

### 対象外
- 暗号資産、投資信託、ETF、REIT、FX
- 株価チャートの表示
- 自動売買・証券会社口座との連携
- 「閾値を上回ったら通知」などの上昇方向のアラート
- 複数ユーザーへの通知、LINE など他の通知手段
- PC が停止・スリープ中の監視
- macOS / Linux 向けの自動起動(GUI 自体は動作するが、サポート対象外)

## 3. 技術スタック

| 項目 | 採用技術 | 備考 |
|------|----------|------|
| 言語 | Python 3.11 以上 | |
| GUI | `tkinter` / `ttk` | Python 標準。追加インストール不要 |
| 株価取得 | `yfinance` | 無料・APIキー不要。`prices.get_current_price(symbol) -> float \| None` に隔離 |
| 日本株の銘柄検索 | JPX「東証上場銘柄一覧」(`data_j.xls`) + `xlrd` | ダウンロードして CSV にキャッシュ。30日ごとに自動更新 |
| 米国株の銘柄検索 | `yfinance.Search` | NYSE / NASDAQ の株式のみ表示 |
| メール通知 | SMTP(標準ライブラリ `smtplib`) | 587: STARTTLS / 465: SSL。Gmail + アプリパスワードを想定 |
| パスワード保存 | `keyring`(Windows 資格情報マネージャー) | 設定ファイルには保存しない |
| 自動起動 | レジストリ `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` | 管理者権限不要 |
| 設定・状態ファイル | JSON | GUI から読み書きするため |

## 4. ディレクトリ構成

```
.
├── README.md               # 利用者向けセットアップ手順(日本語)
├── SPEC.md                 # 本ドキュメント
├── requirements.txt
├── setup.bat               # 仮想環境の作成と依存ライブラリのインストール
├── start.bat               # アプリの起動(コンソール非表示)
├── app.pyw                 # エントリポイント
├── src/stock_alert/
│   ├── core.py             # 設定・状態ファイル、閾値判定、重複通知防止
│   ├── prices.py           # 株価取得
│   ├── search.py           # 銘柄検索(JPX 一覧 / Yahoo Finance)
│   ├── mailer.py           # メール送信、パスワード保存
│   ├── autostart.py        # Windows 自動起動の登録
│   └── gui.py              # 画面
├── tests/                  # 単体テスト・GUI テスト
└── data/                   # 実行時に作成(Git 管理外)
    ├── settings.json       # 監視銘柄・チェック間隔・メール設定(パスワードは含まない)
    ├── state.json          # 通知済みフラグ
    ├── jpx_list.csv        # 東証銘柄一覧のキャッシュ
    └── app.log             # ログ(1MB × 4 世代でローテーション)
```

## 5. 機能要件

| ID | 要件 |
|----|------|
| FR1 | 監視銘柄(シンボル・表示名・閾値)を GUI で追加・閾値変更・削除できる |
| FR2 | 会社名(ひらがな・カタカナ・全角/半角の違いを吸収)、証券コード、ティッカーで銘柄を検索して追加できる。選択した銘柄の現在値を表示し、「現在値の -5% / -10% / -20%」で閾値を入力できる |
| FR3 | アプリの起動中(最小化中を含む)、設定した間隔(既定30分、5〜1440分)で全銘柄の現在値を取得する。「今すぐチェック」で即時実行もできる |
| FR4 | 現在値が閾値 **以下**(`price <= threshold`)の銘柄を検知し、該当銘柄を1通にまとめてメールで通知する |
| FR5 | 同一銘柄について、同一日(JST 基準)に重複して通知しない |
| FR6 | 一度通知した銘柄が閾値を再び上回った場合、当日の通知済みフラグを解除し、再度下回ったときには改めて通知する |
| FR7 | 一部の銘柄で価格取得に失敗しても、残りの銘柄の処理を継続する |
| FR8 | メール送信に失敗した場合は通知済みフラグを立てず、次回チェック時に再送する |
| FR9 | GUI からメール設定を保存し、テストメールを送信できる |
| FR10 | Windows サインイン時に最小化状態で自動起動する設定を GUI から切り替えられる |
| FR11 | 一覧に各銘柄の現在値と状態(監視中 / 閾値以下(通知済み) / 閾値以下(未通知) / 取得失敗 / 未チェック)を表示する |
| FR12 | アプリの二重起動を防止する |

## 6. 状態管理仕様(`state.json`)

- 形式: `{"<symbol>:<YYYY-MM-DD>": true, ...}`(日付は JST)
- 通知を送信できた時点でキーを追加する
- 現在値が閾値を上回っていた場合、当日分のキーを削除する(FR6)
- チェックのたびに7日より古いキーを削除する
- 価格取得に失敗した銘柄のキーは変更しない

## 7. 通知仕様

- 件名: `株価アラート`(テストメールは `株価アラート(テスト)`)
- 送信先が空欄の場合は送信元アドレスに送る
- 本文(複数銘柄は空行区切りで1通にまとめる):

```
【株価アラート】トヨタ自動車(7203.T)
現在値: 2,450
設定した閾値: 2,500 を下回りました。
```

## 8. エラーハンドリング方針

- 株価取得・メール送信・銘柄一覧のダウンロードなど通信を伴う処理はバックグラウンドスレッドで行い、画面を固まらせない
- 個別銘柄の処理中の例外は握りつぶしてログに残し、処理を継続する
- 設定ファイル・状態ファイルが壊れている場合は既定値で起動する。書き込みは一時ファイル経由で行い、途中で PC が落ちてもファイルが壊れないようにする
- 通信エラーなど想定内の失敗は、画面のログ欄とメッセージで分かりやすく伝える

## 9. 受け入れ基準

- [ ] 価格取得をモックしたテストで、閾値以下の銘柄が検知され、メールが1通送られ、`state.json` にフラグが立つ
- [ ] 同じ条件で連続してチェックしても2回目は通知しない(FR5)
- [ ] 閾値を上回ると当日フラグが削除され、再度下回れば通知される(FR6)
- [ ] 一部銘柄の価格取得に失敗しても他の銘柄の処理が継続する(FR7)
- [ ] メール送信失敗時はフラグが立たず、次回再送される(FR8)
- [ ] メール設定が未入力でも例外で落ちず、警告ログが出る
- [ ] GUI で銘柄の検索・追加・閾値変更・削除ができ、`settings.json` に保存される
- [ ] アプリパスワードが `settings.json` に書き込まれない
- [ ] リポジトリ内に認証情報や個人の設定が含まれていない
- [ ] README.md だけを読めば、利用者がゼロからセットアップして動かせる

## 10. 将来の拡張候補

- 市場の営業時間内のみチェックする
- 前日比・移動平均など、固定値以外の条件でのアラート
- Windows のトースト通知、タスクトレイへの常駐
- 通知履歴の蓄積と可視化
